import React from 'react';
import { StyleProvider } from '@providers/all';

const Home = () => {
  return (
    <StyleProvider>
      <div>Hello World!</div>
    </StyleProvider>
  );
};

export default Home;
